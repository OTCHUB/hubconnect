.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn
.env
.env.*
!.env.example
# Public, no-secret build-target defaults (see web/wrangler.jsonc) — `.env.*.local` overrides
# stay ignored by the `.env.*` rule above.
!web/.env.devnet
!web/.env.production
keeper/**/journal/
keeper/keys/
*.log
supabase/.temp/
